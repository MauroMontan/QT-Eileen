
DebianBased: main.py
	pip3 install -r requirements.txt
	sudo apt-get install python3-pyqt5  
	sudo apt-get install pyqt5-dev-tools
	sudo apt-get install qttools5-dev-tools
	echo "all dependencies are good !"